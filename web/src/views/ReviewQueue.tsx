// Replaced by views/Activity.tsx when the app moved to auto-publish
// (Phase 2). Kept as an empty module to avoid a dangling import; the
// human-in-the-loop queue can be restored from git history if needed.
export {};
